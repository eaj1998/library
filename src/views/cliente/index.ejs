<% include ../layouts/topo %>

<div id="content"> 
    <h2>Clientes</h2>         
    <div>
        <a href="cliente/create" class="pull-right btn btn-success m-b-20">Cadastrar Cliente</a>
    </div>
    <div class="row mt-5">
        <div class="col-md-12">
            <table class="table table-bordered table-hover">
                <thead>
                    <th>Código</th>
                    <th>Nome</th>
                    <th>E-mail</th>   
                    <th>Telefone</th>   
                    <th></th>                 
                </thead>
                <tbody>
                    <%
                    if(data){
                        for(var i = 0; i < data.length; i++){
                            %>
                            <tr>
                                <td><%= data[i].codigo %></td>
                                <td><%= data[i].nome %></td>
                                <td><%= data[i].email %></td>
                                <td><%= data[i].telefone %></td>
                                <td>
                                    <a href="cliente/edit/<%= data[i].idcliente %>" class="btn btn-warning">Editar</a>
                                    <a href="cliente/delete/<%= data[i].idcliente %>" class="btn btn-danger">Deletar</a>
                                </td>
                            </tr>
                            <%
                        }
                    }
                    %>                                                   
                </tbody>
            </table>
        </div>
    </div>
</div>

<% include ../layouts/rodape %>

